node_modules
.next
.env
.env.local
*.log

# Real tunnel config holds your tunnel id — commit config.example.yml instead.
cloudflared/config.yml
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo
sim-out
models/*.bin
models/*.onnx
models/*.onnx.json

# Kokoro TTS — commit the code (server.py, sample.py), not the venv/models/samples.
kokoro/venv
kokoro/*.onnx
kokoro/voices-*.bin
kokoro/samples
